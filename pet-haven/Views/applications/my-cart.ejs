<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- Add this: -->
    <link rel="stylesheet" href="/stylesheets/style.css" />

    <title>carts</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>

    <h1>My pets</h1>
    <ul>
      <% pets.forEach(pet => { %> <% if(pet.show!=false){ %>
      <li>
        <a href="/applications/<%= pet._id %>/order-pet">
          <%= pet.petname %>
        </a>
      </li>

      <% }}); %>
    </ul>
  </body>
</html>
